---
interface Props {
  title?: string;
  description?: string;
}
const {
  title = "Frontline — AI-research українською",
  description = "Свіжі AI/ML дослідження українською: TL;DR, контекст для UA IT, лінк на оригінал.",
} = Astro.props;
---

<!doctype html>
<html lang="uk">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <header class="page-header">
      <div class="page-container">
        <a href="/" class="brand"
          ><span class="brand-mark">▎</span>frontline</a
        >
        <nav>
          <a href="/">Стрічка</a>
          <a href="/queue/">Черга</a>
          <a href="/about/">Про</a>
          <a
            href="https://github.com/Moonkeemoo/frontline"
            target="_blank"
            rel="noopener">GitHub</a
          >
        </nav>
      </div>
    </header>
    <main class="page-container">
      <article class="markdown-body">
        <slot />
      </article>
    </main>
    <footer class="page-footer">
      <div class="page-container">
        <p>
          AI/ML дослідження українською. Резюме генеруються автоматично — помилки
          трапляються; <a
            href="https://github.com/Moonkeemoo/frontline/issues">повідомте сюди</a
          >.
        </p>
      </div>
    </footer>

    <style is:global>
      :root {
        --color-canvas: #ffffff;
        --color-canvas-subtle: #f6f8fa;
        --color-fg: #1f2328;
        --color-fg-muted: #59636e;
        --color-border: #d1d9e0;
        --color-border-muted: #d8dee4;
        --color-accent: #0969da;
        --color-success: #1a7f37;
        --color-attention: #9a6700;
        --color-attention-bg: #fff8c5;
        --color-danger: #d1242f;
        --color-danger-bg: #ffebe9;
      }

      @media (prefers-color-scheme: dark) {
        :root {
          --color-canvas: #0d1117;
          --color-canvas-subtle: #161b22;
          --color-fg: #e6edf3;
          --color-fg-muted: #7d8590;
          --color-border: #30363d;
          --color-border-muted: #30363d;
          --color-accent: #2f81f7;
          --color-success: #3fb950;
          --color-attention: #d29922;
          --color-attention-bg: rgba(187, 128, 9, 0.15);
          --color-danger: #f85149;
          --color-danger-bg: rgba(248, 81, 73, 0.15);
        }
      }

      * { box-sizing: border-box; }

      html, body {
        margin: 0;
        padding: 0;
        background: var(--color-canvas);
        color: var(--color-fg);
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans",
          Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
        font-size: 16px;
        line-height: 1.5;
      }

      .page-container {
        max-width: 880px;
        margin: 0 auto;
        padding: 0 24px;
      }

      .page-header {
        border-bottom: 1px solid var(--color-border);
        padding: 16px 0;
      }
      .page-header .page-container {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
      }
      .brand {
        font-weight: 600;
        color: var(--color-fg);
        text-decoration: none;
        font-size: 16px;
        font-family: ui-monospace, SFMono-Regular, "SF Mono", Consolas,
          "Liberation Mono", Menlo, monospace;
      }
      .brand-mark { color: var(--color-accent); margin-right: 4px; }
      .page-header nav {
        display: flex;
        gap: 20px;
        flex-wrap: wrap;
      }
      .page-header nav a {
        color: var(--color-fg-muted);
        text-decoration: none;
        font-size: 14px;
      }
      .page-header nav a:hover {
        color: var(--color-accent);
        text-decoration: none;
      }

      main { padding: 32px 0 64px; }

      .page-footer {
        border-top: 1px solid var(--color-border);
        padding: 24px 0;
        margin-top: 48px;
        font-size: 13px;
        color: var(--color-fg-muted);
      }
      .page-footer p { margin: 0; }

      /* GitHub README markdown rendering ===================================== */
      .markdown-body { word-wrap: break-word; }

      .markdown-body > *:first-child { margin-top: 0 !important; }
      .markdown-body > *:last-child { margin-bottom: 0 !important; }

      .markdown-body h1,
      .markdown-body h2,
      .markdown-body h3,
      .markdown-body h4 {
        margin-top: 24px;
        margin-bottom: 16px;
        font-weight: 600;
        line-height: 1.25;
      }
      .markdown-body h1 {
        font-size: 2em;
        padding-bottom: 0.3em;
        border-bottom: 1px solid var(--color-border-muted);
      }
      .markdown-body h2 {
        font-size: 1.5em;
        padding-bottom: 0.3em;
        border-bottom: 1px solid var(--color-border-muted);
      }
      .markdown-body h3 { font-size: 1.25em; }
      .markdown-body h4 { font-size: 1em; }

      .markdown-body p {
        margin-top: 0;
        margin-bottom: 16px;
      }

      .markdown-body a {
        color: var(--color-accent);
        text-decoration: none;
      }
      .markdown-body a:hover { text-decoration: underline; }

      .markdown-body em { font-style: italic; }
      .markdown-body strong { font-weight: 600; }

      .markdown-body code {
        padding: 0.2em 0.4em;
        margin: 0;
        font-size: 85%;
        background: var(--color-canvas-subtle);
        border-radius: 6px;
        font-family: ui-monospace, SFMono-Regular, "SF Mono", Consolas,
          "Liberation Mono", Menlo, monospace;
      }
      .markdown-body pre {
        padding: 16px;
        overflow: auto;
        font-size: 85%;
        line-height: 1.45;
        background: var(--color-canvas-subtle);
        border-radius: 6px;
        margin-top: 0;
        margin-bottom: 16px;
      }
      .markdown-body pre code {
        padding: 0;
        background: transparent;
        font-size: 100%;
      }

      .markdown-body ul,
      .markdown-body ol {
        padding-left: 2em;
        margin-top: 0;
        margin-bottom: 16px;
      }
      .markdown-body li + li { margin-top: 0.25em; }
      .markdown-body li > p { margin-bottom: 8px; }

      .markdown-body blockquote {
        padding: 0 1em;
        color: var(--color-fg-muted);
        border-left: 0.25em solid var(--color-border);
        margin: 0 0 16px 0;
      }
      .markdown-body blockquote > :first-child { margin-top: 0; }
      .markdown-body blockquote > :last-child { margin-bottom: 0; }

      .markdown-body table {
        display: block;
        width: 100%;
        max-width: 100%;
        overflow: auto;
        border-spacing: 0;
        border-collapse: collapse;
        margin-bottom: 16px;
      }
      .markdown-body table th,
      .markdown-body table td {
        padding: 6px 13px;
        border: 1px solid var(--color-border);
      }
      .markdown-body table th {
        font-weight: 600;
        background: var(--color-canvas-subtle);
      }
      .markdown-body table tr {
        background: var(--color-canvas);
        border-top: 1px solid var(--color-border-muted);
      }
      .markdown-body table tr:nth-child(2n) {
        background: var(--color-canvas-subtle);
      }

      .markdown-body hr {
        height: 0.25em;
        padding: 0;
        margin: 24px 0;
        background: var(--color-border);
        border: 0;
      }

      /* Custom helpers used in our pages ====================================== */
      .meta {
        color: var(--color-fg-muted);
        font-size: 14px;
        font-family: ui-monospace, SFMono-Regular, "SF Mono", Consolas,
          "Liberation Mono", Menlo, monospace;
      }
      .meta .arxiv { color: var(--color-accent); }
      .meta .dot { margin: 0 6px; opacity: 0.5; }

      .tag {
        display: inline-block;
        padding: 0 7px;
        font-size: 12px;
        font-weight: 500;
        line-height: 18px;
        color: var(--color-fg-muted);
        background: var(--color-canvas-subtle);
        border: 1px solid var(--color-border);
        border-radius: 2em;
        font-family: ui-monospace, SFMono-Regular, "SF Mono", Consolas,
          "Liberation Mono", Menlo, monospace;
      }
      .tag-list { display: inline-flex; gap: 4px; flex-wrap: wrap; }

      .alert {
        padding: 8px 16px;
        border-radius: 6px;
        margin-bottom: 16px;
      }
      .alert-attention {
        color: var(--color-attention);
        background: var(--color-attention-bg);
        border: 1px solid var(--color-border);
      }
      .alert-danger {
        color: var(--color-danger);
        background: var(--color-danger-bg);
        border: 1px solid var(--color-border);
      }
      .alert strong { display: block; margin-bottom: 4px; }

      .severity-pill {
        display: inline-block;
        padding: 0 6px;
        font-size: 11px;
        font-weight: 600;
        line-height: 16px;
        border-radius: 3px;
        text-transform: uppercase;
        font-family: ui-monospace, SFMono-Regular, "SF Mono", Consolas,
          monospace;
      }
      .severity-high {
        color: var(--color-canvas);
        background: var(--color-danger);
      }
      .severity-medium {
        color: #ffffff;
        background: var(--color-attention);
      }
      .severity-low {
        color: var(--color-fg-muted);
        background: var(--color-canvas-subtle);
        border: 1px solid var(--color-border);
      }
    </style>
  </body>
</html>
